/*
  The `#ifndef`, `#define` and `#endif` preprocessor directives
  ensure that the file is included only once, preventing multiple
  definition errors.
*/
#ifndef CONSTANTS_H
#define CONSTANTS_H

#define MAX_LINE_LENGTH 80

#endif // CONSTANTS_H
